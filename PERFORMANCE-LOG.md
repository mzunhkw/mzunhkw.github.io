# سجل تحسينات الأداء — mazunhkw.com

توثيق لكل خطوات تحسين سرعة الموقع (PageSpeed / Core Web Vitals)، بالترتيب الزمني، عشان أي تعديل مستقبلي يبني على اللي انسوى مو يكرره أو يلغيه بالغلط.

---

## الجلسة 1 — التشخيص الأول والانتقال إلى Cloudflare

**نقطة البداية:** أداء 67/100 على الجوال، LCP = 7.1 ثانية.

### أسباب المشكلة اللي انلقت
- **بالكود:** صورة المنتج المميز بالرئيسية كانت `loading="eager"` فتنافس شعار الهيرو (عنصر LCP الفعلي) على الشبكة.
- **بالاستضافة:** GitHub Pages ما عنده نقطة حضور قريبة من الكويت، ومدة التخزين المؤقت للأصول قصيرة جدًا (10 دقايق).

### إصلاحات الكود
- إضافة `<link rel="preload">` صريح لصورة الشعار في `layout.tsx`.
- تغيير صورة المنتج المميز من `eager` إلى `lazy` في `ProductCard.tsx`.

### نقل DNS واستضافة الحافة (Cloudflare)
- نقل الدومين من Hostinger إلى Cloudflare (نفس السجلات، Nameservers جديدة، Zone Active).
- تفعيل **Always Use HTTPS**.
- إنشاء **Cache Rule** باسم `Static Assets Long Cache`: يتجاهل `cache-control` الأصلي، Edge TTL = 30 يوم، Browser TTL = يوم واحد، على مسارات `/images/` و `/_next/static/`.
- تفعيل **Proxy (السحابة البرتقالية)** على كل السجلات التسعة.
- تأكيد **SSL = Full** والشهادة Active.

**النتيجة:** تحسّن من 7.1 إلى ~6 ثواني (من طبقة الاستضافة بشكل أساسي، تحسّن جزئي فقط).

---

## الجلسة 2 — رفع كود اللقطة السابقة + فحص جديد

بعد التأكد من رفع تعديلات الجلسة 1 لـ GitHub وتشغيل نشر جديد، فحص PageSpeed جديد على الجوال أعطى:

| المقياس | القيمة | الحالة |
|---|---|---|
| الأداء | 79 | 🟡 |
| LCP | 4.2 ثانية | 🔴 |
| FCP | 3.3 ثانية | 🔴 |
| CLS | 0.03 | 🟢 |
| TBT | 0 ميلي ثانية | 🟢 |
| Speed Index | 3.3 ثانية | 🟢 |

**الفرص المذكورة بالتقرير:** حظر العرض (~2840ms)، تخزين مؤقت فعّال (~583 كيلوبايت)، تحسين عرض الصور (~1033 كيلوبايت).

### التشخيص الفعلي (فحص الكود مباشرة، مو بس التقرير)

1. **شعار الهيدر يحمّل الملف الكامل في كل صفحة.** `Header.tsx` كان يستخدم نفس `logo.webp` (760×694، ~52 كيلوبايت) بينما يعرضه بارتفاع 40-48px فقط بالهيدر — ويتكرر هذا في **كل صفحة بالموقع** (منتجات، تصنيفات، خدمات)، مو بس الرئيسية.

2. **سكربت توليد أحجام الصور (`generate-image-variants.mjs`) كان يترك الملف الأصلي غير مضغوط كخيار بالـ srcset** لما تكون الصورة الأصلية قريبة من أكبر حجم مستهدف (1200px) — وأغلب صور المنتجات 1080-1600px عرض، فكانت تقع بالضبط بهالنطاق. بسبب كثافة بكسل الجوال، المتصفح كان أحيانًا يختار هذا الملف الخام (200-330 كيلوبايت) بدل نسخة مضغوطة أخف (اختبار على صورة وحدة: 335 كيلوبايت مقابل 140 كيلوبايت لو انضغطت صح على مقاس مناسب).

3. **وصف `sizes` لصورة المنتج المميز** كان يبالغ بتقدير العرض المطلوب على الجوال (`100vw` بدون طرح padding الحاوية)، مما يخلي المتصفح يطلب نسخة أكبر من اللازم.

### الإصلاحات المطبّقة

- **`scripts/generate-image-variants.mjs`:** إضافة سقف دائم (`MAX_SERVED_WIDTH = 1000`) — أي صورة يضمن لها نسخة مضغوطة بجودة `VARIANT_QUALITY` حتى لو كانت أبعادها قريبة من الأصل، بدل الاعتماد على الملف الخام كما رُفع.
- **`src/lib/image.ts`:** إزالة إضافة الملف الأصلي كخيار ضمن الـ `srcset` — الاعتماد فقط على النسخ المولَّدة (المضغوطة دائمًا). ملف `src` العادي (fallback) يبقى يشير للأصل، وما يتأثر لأن المتصفحات الحديثة تتجاهله لصالح `srcset` وقت استخدام `sizes`.
- **`src/components/ProductCard.tsx`:** تصحيح `sizes` لصورة المنتج المميز إلى `calc(100vw - 32px)` على الجوال بدل `100vw` (طرح padding الحاوية).
- **شعار الهيدر منفصل عن شعار الهيرو:**
  - ملف جديد `public/logo-header.webp` (176×161، ~10 كيلوبايت) مخصص للهيدر فقط.
  - `src/components/Header.tsx` يستخدم الملف الجديد بدل `logo.webp` الكامل.
  - `src/app/layout.tsx`: الـ `<link rel="preload">` صار يشير لـ `logo-header.webp` (لأنه الموجود بكل صفحة)، بدل ملف الشعار الكبير. شعار الهيرو الكبير بالرئيسية (`hero-logo`) يبقى `fetchPriority="high"` على وسمه مباشرة بدون preload منفصل — كافٍ لأن ماسح الأولوية بالمتصفح يكتشفه فور تحليل الـ HTML.

**النتيجة المتوقعة:** تقليل بايتات محمّلة بكل صفحة (~40+ كيلوبايت أقل لشعار الهيدر وحده على كل صفحة)، وتقليل حجم صور المنتجات المحمّلة فعليًا على الجوال بشكل ملموس.

### خطوات معلّقة (لم تُنفَّذ بعد وقت كتابة هذا السجل)
- [ ] رفع هذه التعديلات لمستودع GitHub ودفعها لفرع `main`.
- [ ] **Cloudflare — Cache Rule جديد يغطي صفحات الـ HTML نفسها** (باستثناء `/admin*`)، مو بس `/images/` و `/_next/static/` — لأن الموقع static بالكامل، وهذا يخلي Cloudflare يخدم الـ HTML من أقرب نقطة بدون رجوع لـ GitHub Pages أصلًا. هذا الاحتمال الأكبر لتفسير أن FCP لسا 3.3 ثانية (قبل تحميل أي صورة).
- [ ] **Cloudflare — تفعيل Early Hints** (Speed → Optimization).
- [ ] فحص PageSpeed جديد بعد الخطوتين أعلاه ومقارنته بـ 79/100 و LCP=4.2s الحاليين.

---

## ملاحظات معمارية دائمة (لا تتغير بدون سبب موثّق)
- الصور الأصلية بـ `public/images/` لا تُعدَّل يدويًا — النسخ المصغّرة (`@NNNw.webp`) وملف `image-manifest.json` يتولّدون تلقائيًا وقت البناء (`prebuild`) وغير مضافين لـ git.
- أي تغيير على `WIDTHS` أو `MAX_SERVED_WIDTH` أو `VARIANT_QUALITY` بـ `generate-image-variants.mjs` يمسح النسخ القديمة المتولّدة محليًا؟ لا — الملفات القديمة بنفس الاسم ما تُعاد كتابتها إذا كانت موجودة (`if (!existsSync(outAbs))`)، فلو غيّرت الجودة أو السقف لازم تحذف مجلد النسخ المولَّدة يدويًا محليًا قبل `npm run build` عشان تتولّد من جديد بالإعداد الجديد (على GitHub Actions ما فيه مشكلة، لأنه بيئة نظيفة بكل مرة).

---

## الجلسة 3 — تشخيص فحص جديد (89/100) + حل الـ CSS الحاجب للعرض

**نقطة البداية:** فحص PageSpeed جديد على الجوال (فحص فعلي على `mazunhkw.com`، الصفحة الرئيسية):

| المقياس | القيمة | الحالة |
|---|---|---|
| الأداء | 89 | 🟡 |
| LCP | 3.4 ثانية | 🟡 |
| FCP | 1.4 ثانية | 🟢 |
| CLS | 0 | 🟢 |
| TBT | 0 ميلي ثانية | 🟢 |
| Speed Index | 4.4 ثانية | 🟡 |

**الفرص المذكورة (بالترتيب اللي حطه التقرير):** طلبات حظر العرض (~730ms)، ميزات JavaScript قديمة (~12 كيلوبايت)، إعادة التدفق الإلزامية، شجرة اعتمادية الشبكة، تحسين عرض الصور (~65 كيلوبايت).

### التشخيص (فحص الكود، بدون وصول لتتبّع Lighthouse الفعلي)

الملاحظة الأهم: فجوة ~2 ثانية بين FCP (1.4s، ممتاز) و LCP (3.4s). هذا نمط معروف: أول رسم للصفحة يصير بسرعة، لكن أكبر عنصر مرئي (سواء نص H1 أو شعار الهيدر) يتأخر لأنه ينتظر شي إضافي. السبب الأرجح: الموقع كامل (كل الصفحات) يحمّل ملف CSS واحد (Tailwind + الأنماط المخصصة) عبر `<link rel="stylesheet">` عادي بـ `<head>` — وهذا **يحجب رسم الصفحة بالكامل** لين يوصل المتصفح يحمّل ويحلّل هذا الملف، حتى لو صورة الهيدر نفسها وصلت مبكر (لأنها Preload). هذا يطابق تمامًا أول فرصة بالتقرير ("طلبات حظر العرض").

`إعادة التدفق الإلزامية` و`شجرة اعتمادية الشبكة` غالبًا أعراض لنفس السبب (سلسلة HTML→CSS→رسم)، متوقع تتحسن أو تختفي لما يُحل حظر الـ CSS.

`ميزات JavaScript القديمة` (~12 كيلوبايت): هذا من حزمة الـ polyfills الداخلية لـ Next.js نفسها (مو من كود الموقع — تم التأكد ما فيه أي سكربت تتبّع/تحليلات بالموقع، لا Google Analytics ولا Meta Pixel ولا غيره). تحسينه يحتاج التعامل مع إعدادات webpack الداخلية لـ Next أو ترقية النسخة — عائد محدود مقابل الجهد، أجّلناه.

`تحسين عرض الصور` (~65 كيلوبايت): نظام الـ srcset/الأحجام المتجاوبة (من الجلسة 2) شغال صح ومُتحقّق منه بالكود (`sizes` لبطاقات `category-work-grid` المضغوطة تطابق فعليًا شبكة عمودين على الجوال). الباقي غالبًا سقف صيغة WebP نفسها — يحتاج صيغة AVIF (أخف من WebP لنفس الجودة تقريبًا) كخطوة تالية منفصلة، ما طُبّقت هالجلسة.

### الإصلاح المطبّق: حقن الـ Critical CSS بعد البناء (postbuild)

جرّبنا أول شي خاصية Next.js الجاهزة `experimental.optimizeCss`، لكن تبيّن أنها **غير موثوقة مع App Router تحديدًا** (نقاش ومشاكل مفتوحة بمستودع Next.js نفسه بخصوصها) — فما استخدمناها، وتفاديناها تمامًا بدل المخاطرة بكسر البناء.

بدلها أضفنا سكربت مخصص `scripts/inline-critical-css.mjs` يشتغل تلقائيًا بعد `next build` (عبر hook باسم `postbuild` بـ`package.json`، بنفس نمط `prebuild` الموجود أصلًا). السكربت:
- يمر على كل ملفات HTML الناتجة فعليًا بمجلد `out/` (كل صفحات الموقع، مو بس الرئيسية).
- يستخدم مكتبة `critters` (أُضيفت لـ `devDependencies`) يحقن فيها الأنماط اللازمة لأول شاشة مباشرة داخل كل صفحة، ويحوّل رابط ملف الـ CSS الكامل الأصلي من حاجب للعرض إلى `preload` + تفعيل بعد التحميل (مع `<noscript>` احتياطي تلقائي لو JS متعطل).
- **`pruneSource: false` مقصودة ومهمة**: الموقع يستخدم ملف CSS فيزيائي واحد مشترك بين كل الصفحات؛ لو انسمح لـ critters يحذف من هذا الملف نفسه (بدل نسخة منفصلة لكل صفحة)، كل صفحة تتعالج بعد الأولى بتلاقي الملف ناقص قواعد تحتاجها هي. خلّها `false` دائمًا إلا إذا صار تحويل الموقع لملف CSS منفصل لكل صفحة (غير متوقع قريبًا).
- لو صفحة معينة فشلت بالمعالجة لأي سبب، السكربت ما يوقف بقية البناء — تلك الصفحة بس ترجع لنفس سلوكها الحالي (CSS حاجب عادي)، عشان ما ننتج build مكسور بالكامل بسبب صفحة وحدة.

### خطوات معلّقة (لم تُنفَّذ بعد وقت كتابة هذا السجل)
- [ ] رفع `package.json` (بعد إضافة `critters` و`postbuild`) و`scripts/inline-critical-css.mjs` الجديد لـ GitHub، ومراقبة أن GitHub Actions يخلص البناء بدون أخطاء.
- [ ] فحص PageSpeed جديد بعد نجاح الـ deploy، ومقارنته بـ 89/100 و LCP=3.4s الحاليين — الهدف تحديدًا تقليل الفجوة بين FCP و LCP.
- [ ] لو الفرصة استمرت أو رجعت: خطوة تالية منفصلة هي إضافة صيغة AVIF جنب WebP (عبر `<picture>`) لصور المنتجات والشعار — تحتاج تعديل `generate-image-variants.mjs` و`image.ts` و`ProductCard.tsx` و`Header.tsx` معًا، لم تُنفَّذ بعد.
- [ ] تنظيف اختياري (لا يؤثر على الأداء، بس نظافة مستودع): ملفات غير مستخدمة بـ `public/` — `logo.png`، `logo.webp`، `logo-hero@508w.webp`، `logo-hero@600w.webp` (يتيمة من قبل حذف شعار الهيرو بالجلسة 2) — وملفين مكرّرين غير مستوردين بأي مكان: `src/data/Header.tsx` و`src/data/Footer.tsx` (نسخ قديمة من المكوّنات الفعلية بـ `src/components/`).
