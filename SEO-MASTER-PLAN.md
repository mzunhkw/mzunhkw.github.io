# SEO MASTER PLAN — مزونة (Mazouna)

> هذا الملف هو المرجع التنفيذي لخطة SEO الخاصة بموقع مزونة.
> تم وضعه داخل مجلد الموقع ليبقى مرتبطًا بالمشروع أثناء التطوير.

## 0. ملخص الاستئناف — اقرأ هذا أولًا قبل أي شيء

هذا القسم موجود ليكفي وحده: لو أُرسل هذا الملف في محادثة جديدة بدون أي شرح إضافي، اقرأ هذا القسم والقسم 14 (حالة التنفيذ) فقط، وتابع التنفيذ مباشرة دون إعادة اكتشاف بنية المشروع أو إعادة سؤال المالك عن قرارات مؤكدة سابقًا.

**بنية المشروع التقنية:**
- Next.js 14 (App Router)، `output: 'export'` (تصدير ساكن)، `trailingSlash: true`.
- منشور على GitHub Pages، دومين مخصص عبر `public/CNAME`.
- صفحات الفئات: `src/app/category/[slug]/page.tsx` (ديناميكي) + نصوص السيو في `src/data/category-seo.ts`. الـ slugs: `sofas`, `majlis`, `cushions`, `bedrooms`, `tables`, `decor`.
- صفحات الخدمات (تنجيد): مسارات ثابتة بمجلد عربي فعلي لكل صفحة، مثل `src/app/تنجيد-كنب-الكويت/page.tsx` (بدون `[slug]` ديناميكي). البيانات والمحتوى الكامل مصدرها الوحيد `src/data/services.ts`، والعرض عبر مكوّن مشترك `src/components/ServiceLandingPage.tsx`.
- `ServicePage` في `src/data/services.ts` تدعم حقلين اختياريين إضافيين: `images` (صور أعمال حقيقية، تُعرض عبر `Gallery` بخاصية `eagerFirst={false}` حتى لا تُحمَّل أول صورة eager وتنافس عنصر LCP الفعلي للصفحة — الصور توضع بصيغة WebP داخل `public/images/<اسم-وصفي>/`) و`pricing` (إما `pricePerMeter` مفرد + `material`، أو `tiers` لعدة تسعيرات حسب نوع القطعة مثل مجلس ظهر/مجلس كنب رسمي — بالإضافة إلى `includes`/`excludes`/`deliveryFee`/`note` الاختيارية، تُبنى منها بيانات `Offer`/`UnitPriceSpecification` تلقائيًا داخل `ServiceLandingPage.tsx`، عرض واحد لكل tier عند وجودها). لا تُضاف هذه الحقول إلا لخدمة تأكد المالك من صورها/سعرها الفعلي. الصفحات الثلاث الحالية مكتملة بهذه البيانات (راجع القسم 14).
- إضافة أي صفحة جديدة (فئة أو خدمة) تتطلب أيضًا تحديث `src/app/sitemap.ts` وإضافة روابط داخلية من/إلى الصفحات ذات الصلة (نفس نمط قسم "خدمات ذات صلة" في صفحات الفئة والخدمة الحالية).

**قرارات مؤكدة من المالك — لا تُعاد الأسئلة عنها:**
- خدمة التنجيد (تنجيد كنب، تنجيد مساند، تنجيد مجالس) متوفرة فعليًا لدى مزونة للثلاثة معًا (مؤكد من المالك).
- لن تُنشأ صفحات مستقلة لـ "كنب/قنفات/مجالس/مساند/غرف نوم الكويت" — هذه الكلمات مخدومة فعليًا عبر `/category/<slug>/` الموجودة، وإنشاء صفحة ثانية بنفس الكلمة يعني Cannibalization (قرار مالك، 2026-09-22). التفاصيل الكاملة والربط بالروابط الفعلية في القسم 14.

**آخر تحديث للحالة:** راجع القسم 14 "حالة التنفيذ" أدناه — يحتوي بالضبط على ما تم إنجازه وما تبقى. آخر خطوة: مراجعة كاملة لخارطة الكلمات (القسم 2) مقابل المحتوى الفعلي (2026-09-22) — تم سد فجوات كلمات مفتاحية في الرئيسية وصفحات `sofas`/`majlis`/`cushions`/`bedrooms`، أبرزها صفحة غرف النوم وربطها بالرئيسية (كانت "غرف نوم" غائبة كليًا عن H1/Title/Meta description للصفحة الرئيسية رغم كونها أعلى كلمة طلبًا في القسم 2). التفاصيل الكاملة أدناه.

**قاعدة عمل دائمة لأي جلسة قادمة:** بعد تنفيذ أي خطوة، حدّث هذا القسم (0) والقسم 14 في نفس الجلسة قبل تسليم الملف/الـ zip. لا تترك الحالة موثّقة في مكان آخر غير هذا الملف.

## 1. الهدف

رفع الظهور العضوي لمزونة في نتائج البحث داخل الكويت، وتحويل البحث غير المدفوع إلى زيارات واستفسارات وعملاء، مع التركيز على نية البحث التجارية والمحلية.

**مصادر البيانات:**
- بيانات الطلب السوقي: بحث الكلمات المفتاحية للسوق الكويتي.
- بيانات أداء مزونة الفعلية: Google Search Console الخاص بمزونة عند توفره.
- ملاحظة: Google Search Console يقيس أداء موقع مزونة، وليس حجم السوق الكلي.

## 2. Keyword Map

### P1 — الأولوية الرئيسية

| الكلمة | الطلب الشهري التقديري | الصفحة المستهدفة |
|---|---:|---|
| تنجيد كنب | 1,600 | صفحة خدمة مستقلة إذا كانت الخدمة متوفرة |
| غرف نوم | 880 | صفحة غرف النوم |
| كنب | 480 | صفحة الكنب |
| غرفة نوم | 390 | صفحة غرف النوم |
| غرف نوم مودرن | 390 | صفحة غرف النوم / صفحة فرعية عند وجود محتوى ومنتجات كافية |
| قنفات | 320 | صفحة القنفات |
| مساند | 320 | صفحة المساند |
| تسريحة غرفة نوم | 320 | صفحة/منتج فقط إذا كانت مزونة تبيعها |
| كنبات | 140 | صفحة الكنب، وليس صفحة منفصلة |
| تفصيل كنب | 110 | صفحة تفصيل كنب إذا كانت الخدمة متوفرة |
| غرف نوم كاملة | 210 | صفحة غرف النوم/مجموعة منتجات إذا كانت متوفرة |
| تفصيل غرف نوم | 70 | صفحة الخدمة إذا كانت متوفرة |
| مجالس | 70 | صفحة المجالس |
| قنفات مودرن | 70 | صفحة القنفات أو قسم داخلها |

### P2 — الأولوية الثانية

- كنب مودرن
- كنب زاوية
- تلبيس قنفات
- تنجيد أثاث
- تصميم غرف نوم
- غرفة نوم مودرن
- غرف نوم كلاسيك
- غرف نوم أطفال
- غرف أطفال
- كنبة سرير
- غطاء كنب
- تفصيل مجالس
- مجالس عربية
- مجالس أرضية
- مساند مجالس

> لا يتم إنشاء صفحة مستقلة لكل اختلاف إملائي مثل: كنب / كنبات / كنفات عندما تكون نية البحث واحدة. تُستخدم هذه الصيغ طبيعيًا داخل الصفحة الرئيسية للموضوع لتجنب Cannibalization.

## 3. بنية الموقع المستهدفة

```text
مزونة
├── كنب
│   ├── كنب مودرن
│   ├── تفصيل كنب
│   └── كنب زاوية
├── قنفات
│   └── قنفات مودرن
├── مجالس
│   ├── مجالس عربية
│   ├── مجالس أرضية
│   └── تفصيل مجالس
├── مساند
│   └── مساند مجالس
├── غرف نوم
│   ├── غرف نوم مودرن
│   ├── غرف نوم كاملة
│   └── غرف نوم تفصيل
├── أعمالنا
├── المدونة
└── تواصل معنا
```

**قاعدة مهمة:** لا تُنشأ صفحة إلا إذا كان لها منتج/خدمة/محتوى حقيقي ومميز داخل مزونة.

## 4.0 صفحات الخدمات المستقلة التي تمت إضافتها للخطة

هذه الخدمات تستحق صفحات مستقلة لأنها تمثل نوايا بحث تجارية مختلفة:

1. **تنجيد كنب الكويت** — `/تنجيد-كنب-الكويت/`
2. **تنجيد مساند الكويت** — `/تنجيد-مساند-الكويت/`
3. **تنجيد مجالس الكويت** — `/تنجيد-مجالس-الكويت/`

كل صفحة يجب أن تحتوي على محتوى أصلي، صور أعمال حقيقية، تفاصيل الخدمة، دعوة للتواصل، وأسئلة شائعة حقيقية. لا يتم نشر ادعاءات عن خدمة غير متوفرة فعليًا.

## 4. صفحات SEO الأساسية

### كنب
- Target: كنب الكويت، كنب، كنبات، كنفات، كنب مودرن
- H1: كنب في الكويت – مودرن وتفصيل حسب الطلب
- Title المقترح: كنب الكويت | كنب مودرن وتفصيل حسب الطلب | مزونة

### قنفات
- Target: قنفات الكويت، قنفات، كنفات، قنفات مودرن
- Title: قنفات الكويت | قنفات مودرن وتفصيل حسب الطلب | مزونة

### مجالس
- Target: مجالس الكويت، مجالس، تفصيل مجالس، مجالس عربية، مجالس أرضية
- Title: مجالس الكويت | مجالس عربية وأرضية تفصيل حسب الطلب | مزونة

### مساند
- Target: مساند الكويت، مساند، مساند مجالس، مساند ظهر
- Title: مساند الكويت | مساند مجالس وظهر تفصيل حسب الطلب | مزونة

### غرف النوم
- Target: غرف نوم الكويت، غرف نوم، غرفة نوم، غرف نوم مودرن، غرف نوم كاملة
- H1: غرف نوم في الكويت
- Title: غرف نوم الكويت | غرف نوم مودرن وتفصيل حسب الطلب | مزونة

### تنجيد الكنب
- Target: تنجيد كنب الكويت، تجديد كنب، تغيير قماش كنب، تنجيد كنبات، تنجيد قنفات
- **تُنشأ فقط إذا كانت الخدمة متوفرة فعليًا لدى مزونة.**
- Title: تنجيد كنب الكويت | تجديد وتغيير قماش الكنب | مزونة

## 5. On-Page SEO

لكل صفحة تجارية:
- Title فريد.
- Meta description فريدة وموجهة لنِيّة البحث.
- H1 واحد واضح.
- H2s تغطي الأسئلة والخصائص المهمة.
- نص أصلي وليس إعادة صياغة لمنافس.
- صور أصلية مع ALT وصفي.
- معلومات المنتج/الخدمة: المقاسات، الخامات، الألوان، الخيارات، طريقة الطلب، مناطق التوصيل عند توفرها.
- CTA واضح: طلب عرض سعر / واتساب / تواصل.
- FAQ حقيقي مبني على أسئلة العملاء.
- روابط داخلية للصفحات المرتبطة.

## 6. Internal Linking

النموذج:
- الرئيسية → الكنب / القنفات / المجالس / المساند / غرف النوم.
- الكنب → كنب مودرن / تفصيل كنب / كنب زاوية عند وجود الصفحات.
- القنفات → قنفات مودرن.
- المجالس → مجالس عربية / أرضية / تفصيل.
- غرف النوم → مودرن / كاملة / تفصيل.
- المنتجات → الفئة + المنتجات ذات الصلة + التواصل.

يجب استخدام Anchor Text طبيعي ومتنوّع وعدم حشو الكلمة المفتاحية.

## 7. Structured Data

عند توفر البيانات الفعلية:
- Organization / LocalBusiness.
- BreadcrumbList.
- Product وOffer للمنتجات المناسبة.
- ProductGroup/variants عندما توجد Variants حقيقية.
- FAQPage فقط عندما تكون الأسئلة والأجوبة ظاهرة فعليًا للمستخدم.

لا تُضاف بيانات منظمة لمنتجات أو خدمات غير موجودة.

## 8. Technical SEO

قائمة التنفيذ:
- Canonical صحيح لكل صفحة.
- Sitemap XML محدث.
- Robots.txt صحيح.
- منع فهرسة صفحات البحث الداخلي والفلاتر غير المفيدة.
- صفحات متوافقة مع الجوال.
- تحسين Core Web Vitals.
- ضغط الصور واستخدام WebP/AVIF عند الإمكان.
- Lazy loading للصور غير الظاهرة.
- روابط قابلة للزحف.
- عدم وجود صفحات 404 داخلية.
- Redirects صحيحة عند تغيير الروابط.
- تجنب الصفحات المكررة.

## 9. Local SEO — الكويت

يجب إبراز:
- الكويت كموقع الخدمة/التوصيل إذا كان ذلك صحيحًا.
- مناطق التغطية الفعلية.
- بيانات التواصل الحقيقية.
- اسم النشاط كما هو مستخدم فعليًا.
- صور حقيقية للمحل/المنتجات/الأعمال.
- Google Business Profile عند توفره.

لا يتم إنشاء عشرات الصفحات مثل:
«كنب الفروانية»، «كنب حولي»، «كنب السالمية» لمجرد استهداف أسماء المناطق إذا لم يكن لكل صفحة قيمة محلية حقيقية.

## 10. Content Plan

موضوعات مقترحة:
1. أفضل أنواع أقمشة الكنب للاستخدام اليومي.
2. كيف تختار كنب مناسب لمساحة الصالة؟
3. الفرق بين تفصيل الكنب والتنجيد.
4. كيف تختار مقاس الكنب؟
5. أفضل ألوان الكنب للمجالس.
6. أفكار وتصاميم مجالس.
7. المجالس الأرضية: المزايا والاستخدامات.
8. كيف تختار تصميم غرفة النوم؟
9. ألوان غرف النوم الحديثة.
10. تصاميم غرف نوم مودرن.

كل مقال يجب أن يخدم صفحة تجارية ويحتوي روابط داخلية مناسبة.

## 11. 90-Day Execution Plan

### الأيام 1–14
- تدقيق تقني.
- تثبيت خريطة الكلمات.
- تثبيت Canonical/Sitemap/Robots.
- تحسين الصفحة الرئيسية.
- تجهيز قوالب Title/H1/Meta.
- تجهيز Schema.

### الأيام 15–30
- تحسين صفحة الكنب.
- تحسين صفحة القنفات.
- تحسين صفحة المساند.
- تحسين صفحة المجالس.

### الأيام 31–45
- تحسين صفحة غرف النوم.
- صفحات فرعية فقط عند وجود منتجات/خدمات كافية.
- تحسين الصور والـ ALT.
- بناء روابط داخلية.

### الأيام 46–60
- إنشاء/تحسين صفحة تفصيل الكنب إذا كانت الخدمة متوفرة.
- إنشاء/تحسين صفحة تفصيل المجالس إذا كانت الخدمة متوفرة.
- تحسين صفحات المنتجات.

### الأيام 61–90
- نشر محتوى داعم.
- مراجعة الكلمات التي بدأت بالظهور.
- تحسين CTR للصفحات ذات الظهور العالي والنقر المنخفض.
- تحديث المحتوى بناءً على Search Console.
- بناء إشارات محلية وروابط ذات صلة.

## 12. KPIs

تتم المتابعة شهريًا:
- Organic Clicks.
- Organic Impressions.
- CTR.
- Average Position.
- عدد الكلمات في Top 3.
- عدد الكلمات في Top 10.
- Organic leads.
- WhatsApp/contact conversions.
- أداء كل صفحة تجارية.
- صفحات فقدت الظهور أو النقرات.

## 13. قواعد عدم الانحراف عن الخطة

1. لا ننشئ صفحات Keyword Variants متطابقة.
2. لا نستهدف كلمات لا تتطابق مع منتجات/خدمات مزونة.
3. لا نضع أحجام البحث كأنها أرقام مضمونة للزيارات.
4. لا نعتمد على GSC لموقع آخر لقياس طلب السوق لمزونة.
5. نستخدم بيانات GSC الخاصة بمزونة لقياس الأداء بعد ربطها.
6. أي صفحة جديدة يجب أن يكون لها Search Intent واضح وقيمة حقيقية للمستخدم.

## 13.5 صفحات الخدمات الجديدة

تم تجهيز المحتوى SEO والـ route manifest للخدمات التالية داخل `seo-services/`:
- تنجيد كنب الكويت
- تنجيد مساند الكويت
- تنجيد مجالس الكويت

ملاحظة: ملفات Markdown وJSON هي مصدر المحتوى والتنظيم. يجب ربطها بمكونات/Routes الموقع الفعلية قبل اعتبار الصفحات منشورة للمستخدم.

## 14. حالة التنفيذ

### تم إدخاله في المشروع
- هذا الملف: `SEO-MASTER-PLAN.md`
- خارطة الكلمات.
- بنية الصفحات المستهدفة.
- أولويات التنفيذ.
- قواعد On-Page SEO.
- Technical SEO checklist.
- Local SEO.
- Content Plan.
- Internal Linking.
- Structured Data guidance.
- خطة 90 يوم.
- KPIs.

### تم إدخاله في المشروع (Phase 2 — تنفيذ خطوة صفحات الخدمات)
- إنشاء صفحات الخدمات الثلاث كـ Routes فعلية في `src/app/`:
  - `/تنجيد-كنب-الكويت/`
  - `/تنجيد-مساند-الكويت/`
  - `/تنجيد-مجالس-الكويت/`
- كل صفحة تحتوي: Title/Meta فريدة، H1، H2s، محتوى أصلي من `seo-services/*.md`، CTA واتساب، FAQ ظاهر للمستخدم، Breadcrumb.
- Structured Data لكل صفحة خدمة: Service، FAQPage، BreadcrumbList (عبر `src/components/ServiceLandingPage.tsx`).
- إضافة الصفحات الثلاث إلى `sitemap.xml` (`src/app/sitemap.ts`).
- Internal Linking:
  - الرئيسية → قسم "خدمات التنجيد" يربط الصفحات الثلاث.
  - صفحة كنب (`/category/sofas/`) → تنجيد كنب.
  - صفحة مساند (`/category/cushions/`) → تنجيد مساند.
  - صفحة مجالس (`/category/majlis/`) → تنجيد مجالس.
  - كل صفحة خدمة → القسم المرتبط + الخدمتين الأخريين.
- تأكيد الملاك: خدمة التنجيد (كنب/مساند/مجالس) متوفرة فعليًا لدى مزونة (تم التأكيد قبل التنفيذ).

### تم إدخاله في المشروع (2026-09-22 — صور أعمال حقيقية وتسعير لصفحة تنجيد مساند)
- إضافة 4 صور أعمال حقيقية (مجالس/مساند منفذة فعليًا) إلى صفحة `/تنجيد-مساند-الكويت/`: محوّلة إلى WebP مضغوط (900px عرض، ~25–68KB للصورة) في `public/images/masaned-real-work/`، مع ALT وصفي فريد لكل صورة.
- قسم "أعمالنا الحقيقية" يُعرض عبر مكوّن `Gallery` الموجود مسبقًا (نفس مكوّن صفحة المنتج)، لكن بخاصية جديدة `eagerFirst={false}` حتى تبقى كل صور المعرض lazy-load ولا تنافس عنصر LCP الفعلي للصفحة (H1/النص أعلى الصفحة) — لا تأثير على صفحات المنتج القائمة لأن `eagerFirst` تبقى `true` افتراضيًا هناك.
- إضافة تسعير حقيقي مؤكد من المالك لخدمة تنجيد المساند فقط: 5 دنانير للمتر، خامة "قماش شانيل تركي"، مع ملاحظة أن مندوب مزونة يصل للقياس ومعاينة الخامة قبل التنفيذ. يظهر في قسم مخصص بالصفحة + FAQ جديد ("كم سعر تنجيد المساند؟") + بيانات `Offer`/`UnitPriceSpecification` منظمة.
- لم تُضف صور أو أسعار لصفحة تنجيد مجالس — لا تتوفر بيانات فعلية مؤكدة لها بعد (تُضاف لاحقًا فور توفرها، بنفس الآلية).

### تم إدخاله في المشروع (2026-09-22 — صور أعمال حقيقية وتسعير لصفحة تنجيد كنب)
- إضافة 3 صور أعمال حقيقية (كنب منفذ فعليًا) إلى صفحة `/تنجيد-كنب-الكويت/`: WebP مضغوط (900px، ~28–94KB) في `public/images/kanab-real-work/`، بنفس آلية `Gallery` + `eagerFirst={false}` المستخدمة لصفحة تنجيد مساند.
- تسعير حقيقي مؤكد من المالك: 19 دينار للمتر، خامة قماش تركي حسب رغبة العميل (شانيل/مخمل/شامواه)، ورسوم توصيل 15 دينار.
- أُضيف حقل جديد اختياري `deliveryFee` لنوع `ServicePricing` في `src/data/services.ts` (رسوم توصيل منفصلة عن ملاحظة `note` الحرة، ليست كل خدمة لديها توصيل مدفوع). يظهر في قسم "السعر والخامة" بصفحة الخدمة فقط عند توفره.
- FAQ جديد "كم سعر تنجيد الكنب؟" بنفس نمط صفحة تنجيد مساند.
- صفحتا تنجيد مساند وتنجيد كنب أصبحتا مكتملتان بصور وتسعير حقيقي. المتبقي: تنجيد مجالس فقط.

### تم إدخاله في المشروع (2026-09-22 — صور أعمال حقيقية وتسعير لصفحة تنجيد مجالس، آخر صفحة متبقية)
- إضافة 4 صور أعمال حقيقية إلى صفحة `/تنجيد-مجالس-الكويت/`: WebP مضغوط (900px، ~15–29KB) في `public/images/majlis-real-work/`، بنفس آلية `Gallery` + `eagerFirst={false}`.
- تسعير حقيقي مؤكد من المالك بتسعيرتين مختلفتين حسب نوع المجلس (وليس سعرًا واحدًا كباقي الخدمتين): مجلس ظهر 13 دينار/متر، مجلس كنب رسمي 28 دينار/متر. يشمل السعر: إصلاح الإسفنج، صبغ الخشب، تبديل الخامة. لا يشمل: تكاليف النقل والتركيب.
- توسيع نوع `ServicePricing` في `src/data/services.ts` ليدعم تعدد التسعيرات: حقل `tiers` (مصفوفة `{label, pricePerMeter}`) كبديل عن `pricePerMeter` المفرد، بالإضافة إلى `includes`/`excludes` (قوائم نصية لما يشمله/لا يشمله السعر). `pricePerMeter` و`material` أصبحا اختياريين بالكامل للتوافق العكسي مع صفحتي تنجيد كنب وتنجيد مساند القائمتين (لم يتغير عرضهما).
- بيانات Offer المنظمة تدعم الآن مصفوفة عروض (Offer واحد لكل تسعيرة) عند وجود `tiers`، عبر دالة `buildOffer` مشتركة داخل `ServiceLandingPage.tsx`.
- عنوان قسم السعر في الصفحة تغيّر من "السعر والخامة" إلى "السعر والتفاصيل" (أشمل، يغطي حالة عدم وجود خامة واحدة محددة مثل تنجيد المجالس).
- **الحالة النهائية: صفحات الخدمات الثلاث (تنجيد كنب، تنجيد مساند، تنجيد مجالس) مكتملة بصور أعمال حقيقية وتسعير حقيقي مؤكد من المالك.** لا مهام صور/تسعير متبقية لصفحات الخدمات الحالية.

### تم إدخاله في المشروع (2026-09-22 — مراجعة خارطة الكلمات مقابل المحتوى الفعلي وسد الفجوات)
راجعت خارطة الكلمات (القسم 2) مقابل المحتوى الظاهر فعليًا في الرئيسية وصفحات `src/data/category-seo.ts` (المصدر الوحيد لنصوص صفحات `/category/<slug>/`) وصفحات الخدمات (`src/data/services.ts` — كانت مطابقة للخطة أصلًا، لا تعديل عليها). التعديلات التالية على المحتوى الظاهر فقط، بدون إنشاء أي صفحة جديدة وبدون أي ادعاء منتج/خدمة غير متوفرة:

- **الصفحة الرئيسية (`src/app/page.tsx`, `src/data/site-config.ts`)** — الفجوة الأهم: كلمة "غرف نوم" (880 طلب شهري، أعلى كلمة في القسم 2) كانت غائبة كليًا عن H1 وTitle وMeta description للرئيسية، وعن فقرة الروابط الداخلية الختامية (رغم وجودها في شبكة "أقسام المعرض" فقط). أُضيفت إلى: `seoTitle`/`seoDescription` في site-config.ts، H1 الرئيسية، H2 الفقرة الختامية، وأُضيف رابط داخلي صريح لـ `/category/bedrooms/` في الفقرة الختامية (يطابق قاعدة القسم 6: الرئيسية → غرف النوم).
- **صفحة غرف النوم (`category-seo.ts` → `bedrooms`)** — كانت الأضعف محتوى (فقرة تعريفية واحدة، FAQ فارغة تمامًا). أُضيفت الكلمات الناقصة "غرفة نوم"، "غرف نوم مودرن"، "غرف نوم كاملة" إلى Title/Description/intro، وأُضيف FAQ حقيقي (سؤالان) كان غائبًا بالكامل. لم تُضف كلمة "تسريحة غرفة نوم" (موجودة بالقسم 2) لعدم وجود منتج تسريحة فعلي في `products.json`، ولم تُضف صفحة فرعية "غرف نوم مودرن" لعدم توفر منتجات كافية حاليًا (تصنيف bedrooms بدون منتجات منشورة بعد) — يطابق شرط القسم 4 "صفحة فرعية عند وجود محتوى ومنتجات كافية".
- **صفحة كنب/قنفات (`sofas`)** — أُضيفت "كنب مودرن" (يطابق منتجًا فعليًا موجودًا: "كنب مودرن تفصيل منطقة سلوى") و"قنفات مودرن" إلى Title/H1/Description، وأُضيفت الصيغ الإملائية "كنبات"/"كنفات" داخل نص الصفحة (وليس Title منفصل) حسب قاعدة القسم 2 الصريحة بعدم إنشاء/استهداف صفحة منفصلة لكل اختلاف إملائي.
- **صفحة مجالس (`majlis`)** — أُضيفت عبارة "تفصيل مجالس" حرفيًا إلى Title وintro (كانت الصفحة تستخدم "تفصيل" بدون اقتران مباشر بكلمة "مجالس").
- **صفحة مساند (`cushions`)** — أُضيفت "مساند ظهر" إلى Description وintro (يطابق منتجًا فعليًا موجودًا: "مساند ظهر جابر الاحمد").
- تحقّق: `npm run build` نجح بدون أخطاء (20/20 صفحة)، وتم التأكد يدويًا من ظهور الكلمات في HTML المُصدَّر (Title/Meta) لكل من الرئيسية وصفحة bedrooms وsofas.
- لم يُلمس: صفحات الخدمات الثلاث (services.ts) — كانت مطابقة تمامًا لأهداف القسم 4/4.0 أصلًا. صفحتا `tables`/`decor` — غير مدرجتين أصلًا في خارطة الكلمات بالقسم 2، فلم تُلمسا تجنبًا لإضافة كلمات غير مخطط لها.

### تم إدخاله في المشروع (2026-09-22 — عرض أفقي لآخر الأعمال بالرئيسية + ربط دائم لصفحات التنجيد)
- **الرئيسية (`src/app/page.tsx`)**: استُبدل قسم "مختارات" (شبكة مسطّحة لكل المنتجات المميزة بلا تصنيف) بقسم "أحدث الأعمال من كل قسم": لكل تصنيف فيه منتجات فعليًا (حاليًا: كنب وقنفات، مجالس، مساند)، صف أفقي قابل للتمرير (`overflow-x-auto` + `snap-x`) يعرض آخر 3 منتجات من ذلك التصنيف (بترتيب الإضافة بـ `products.json`، الأحدث أولًا)، مع رابط "عرض الكل" لصفحة التصنيف. تصنيفات `bedrooms`/`tables`/`decor` بدون منتجات حاليًا فلا تظهر لها صفوف (تلقائيًا عبر فلترة `visibleProducts`)، ستظهر فور نشر منتجات فيها بدون أي تعديل إضافي.
- **ربط صفحات التنجيد بالرئيسية (`src/components/Header.tsx`)**: كان الـ Header (الموجود بكل صفحات الموقع عبر `layout.tsx`) يحتوي فقط رابطي "الرئيسية" و"المنتجات" — الوصول لصفحات التنجيد الثلاث كان ممكنًا فقط من داخل الرئيسية نفسها أو من صفحات الفئات المرتبطة (`sofas`/`majlis`/`cushions`). أُضيف رابط ثابت "خدمات التنجيد" في الـ Header يشير إلى `/#خدمات-التنجيد` (قسم بالرئيسية أُضيف له `id`)، فيصل الزائر لقسم التنجيد من أي صفحة بالموقع بنقرة واحدة. صفحات الخدمات الثلاث نفسها كانت أصلًا ترتبط بالرئيسية عبر Breadcrumb — لا تعديل مطلوب هناك.
- تحقّق: `npm run build` نجح (20/20 صفحة)، وتأكدت من ظهور الـ id/الروابط في HTML المُصدَّر للرئيسية.

### تم إدخاله في المشروع (2026-09-22 — زر واتساب عائم بكل صفحات الموقع)
- مكوّن جديد `src/components/FloatingWhatsApp.tsx`: زر دائري عائم (أخضر واتساب، أيقونة SVG مضمّنة، بدون طلب شبكة خارجية) يفتح محادثة واتساب مباشرة برسالة تعريفية جاهزة، عبر نفس رقم `siteConfig.whatsappNumber` المستخدم بباقي الموقع.
- أُضيف في `src/app/layout.tsx` (بعد `<Footer />`) فيظهر تلقائيًا بكل صفحات الموقع الثابتة (Static Export) بدون الحاجة لإضافته يدويًا بكل صفحة: الرئيسية، صفحات الفئات الست، صفحات المنتجات، وصفحات التنجيد الثلاث.
- موضعه: أسفل يسار الشاشة (`fixed bottom-5 left-5`)، بحيث لا يتعارض مع أي عنصر أسفل الصفحة — الفوتر أصلًا فيه هامش سفلي إضافي بالجوال (`pb-24`) كان مخصصًا لعنصر عائم من هذا النوع.
- تحقّق: `npm run build` نجح (20/20 صفحة)، وتأكدت من ظهور الزر مرة واحدة بالضبط بالـ HTML المُصدَّر لعينة من الصفحات (الرئيسية، فئة، صفحة خدمة، صفحة المنتجات).

### يحتاج تنفيذًا لاحقًا
- ربط Google Search Console الخاص بمزونة.
- مراجعة نتائج الفهرسة والأداء بعد الإطلاق (بما فيها صفحات الخدمات الجديدة).
- تحديث الخطة بالأرقام الفعلية من GSC.
- بناء محتوى صور أعمال حقيقية + تسعير لصفحة تنجيد مجالس (الوحيدة المتبقية — تنجيد كنب وتنجيد مساند مكتملتان، راجع البنود أعلاه). **تم إنجازه بتاريخ 2026-09-22 — الصفحات الثلاث مكتملة الآن.**

### قرار: صفحات الفئات المستقلة (/كنب-الكويت/ إلخ) — لن تُنفَّذ
كانت الخطة تقترح إنشاء `/كنب-الكويت/`، `/قنفات-الكويت/`، `/مجالس-الكويت/`، `/مساند-الكويت/`، `/غرف-نوم-الكويت/` كصفحات مستقلة (حسب `seo-services/PHASE-2-URL-MAP.json`). تقرر عدم إنشائها لأن صفحات `/category/<slug>/` الموجودة فعليًا في المشروع تستهدف نفس الكلمات بالضبط — وإنشاء صفحة ثانية بنفس الكلمة يعني Duplicate Content/Cannibalization، وهو ممنوع بنص هذه الخطة (القسم 8، والقاعدة 1 في القسم 13). قرار المالك بتاريخ 2026-09-22.
الربط الفعلي للكلمات:
- كنب الكويت / قنفات الكويت → `/category/sofas/`
- مجالس الكويت → `/category/majlis/`
- مساند الكويت → `/category/cushions/`
- غرف نوم الكويت → `/category/bedrooms/`
هذا البند مغلق نهائيًا ولا يُعاد فتحه إلا بقرار جديد صريح من المالك.

---
**الإصدار:** v1.0  
**المشروع:** Mazouna / مزونة  
**السوق:** الكويت  
**الهدف:** SEO + Local Intent + Commercial Intent


## Phase 2 — صفحات تجارية وربط داخلي

تم الانتقال إلى مرحلة تحويل خارطة الكلمات إلى صفحات تجارية فعلية:
- صفحات خدمات التنجيد الثلاث.
- تقوية صفحات الكنب والقنفات والمجالس والمساند وغرف النوم عند وجود المنتجات/الخدمات.
- بناء Internal Linking بين الخدمات والفئات والمنتجات.
- مراجعة Sitemap وCanonical وSchema لكل صفحة.

ملف التنفيذ: `SEO-PHASE-2-IMPLEMENTATION.md`
خريطة الروابط: `seo-services/PHASE-2-URL-MAP.json`

