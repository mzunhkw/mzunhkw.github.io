import { Category } from '@/lib/types';
import categoriesData from './categories.json';

// عدّل categories.json مباشرة لإضافة/تعديل تصنيف.
export const categories: Category[] = categoriesData as Category[];
